########################################################
## Caution
##
## Don't edit these settings as they live with the repo
## Put your settings in the shared path on the server.
########################################################

ActionMailer::Base.smtp_settings = {
  :address  => 'mail.texashan.org',
  :domain => 'talho.org',
  :authentication => :plain,
  :user_name => 'openphin@talho.org',
  :password => 'fake',
}

ExceptionNotification::Notifier.exception_recipients = %w(fake@talho.org)
ExceptionNotification::Notifier.email_prefix = "[OpenPHIN] "
ExceptionNotification::Notifier.sender_address = %("Application Error" <fake@talho.org>)