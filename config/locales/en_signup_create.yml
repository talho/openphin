# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en_signup_create:
    activerecord:
        errors:
            messages:
                  # Default messages: complete sentences
                  inclusion: "is not included in the list"
                  exclusion: "is reserved"
                  invalid: "is invalid"
                  confirmation: "doesn't match confirmation"
                  accepted: "must be accepted"
                  empty: "can't be empty"
                  blank: "can't be blank"
                  too_long: "is too long (maximum is {{count}} characters)"
                  too_short: "is too short (minimum is {{count}} characters)"
                  wrong_length: "is the wrong length (should be {{count}} characters)" 
                  taken: "has already been taken"
                  not_a_number: "is not a number"
                  greater_than: "must be greater than {{count}}"
                  greater_than_or_equal_to: "must be greater than or equal to {{count}}"
                  equal_to: "must be equal to {{count}}"
                  less_than: "must be less than {{count}}"
                  less_than_or_equal_to: "must be less than or equal to {{count}}"     
                  odd: "must be odd"
                  even: "must be even"            
            template:
                header:
                    one: "1 error prohibited your signup."
                    other: "{{count}} errors prohibited your signup."
                body: "There were problems with the following items:"
            models:
                user:
                    attributes:
                        role_requests:
                            invalid: "^You must select a home jurisdiction.  You must also select a role if you are a health professional."
                role_request:        
                    attributes:
                        role_requests_role:   
                            blank: "^Role needs to be selected."
                        role_requests_jurisdiction:
                            blank: "^Home Jurisdiction needs to be selected."   
             
