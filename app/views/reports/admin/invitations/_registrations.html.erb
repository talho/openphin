<%= content_tag(:h3, "Registrations complete: #{regs["complete"]["percentage"]}% (#{regs["complete"]["total"]})") %>
<%= content_tag(:h3, "Registrations incomplete: #{regs["incomplete"]["percentage"]}% (#{regs["incomplete"]["total"]})" ) %>
